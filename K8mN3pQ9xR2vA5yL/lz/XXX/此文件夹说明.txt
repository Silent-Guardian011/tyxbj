【此文件夹是放纯颜色色的文件】

bt 是磁力链接，种子失效快，一般不用，可以删掉

db 是一些数据库，是各位大佬打包的，可以通过万能王和资源管理查看

json  放开头是含有播放链接list[]的json文件

m3u txt 放里面有播放链接格式的相应文件

php py 放对应的脚本文件

以上文件夹文件(py和php除外)都可以用资源管理，万能王，或者对应的脚本查看。

此文件夹内的php，py默认隐藏，在文件夹后面加标签[秘]即可显示php[秘]，py[秘]

遇到以上对应类型的文件直接丢进去就可以了。